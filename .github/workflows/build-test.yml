name: Build, Lint, and Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-lint-test:
    name: Build, Lint, and Test
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - uses: actions/checkout@v4
      - name: Uninstall globally installed Yarn
        run: sudo npm uninstall -g yarn
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
          cache-dependency-path: yarn.lock
          check-latest: true
      - name: Enable Corepack
        run: corepack enable
      - name: Set Yarn version
        run: corepack prepare yarn@4.3.1 --activate
      - name: Add Yarn to PATH
        run: echo "$(dirname $(which yarn))" >> $GITHUB_PATH
      - name: Print PATH
        run: echo $PATH
      - name: Print Yarn binary location
        run: which yarn
      - name: Verify Yarn executable
        run: |
          if [ -x "$(command -v yarn)" ]; then
            echo "Yarn is executable"
          else
            echo "Yarn is not executable"
            exit 1
          fi
      - name: Get Yarn cache directory
        run: echo "::set-output name=YARN_CACHE_DIR::$(yarn cache dir)"
        id: yarn-cache-dir
      - name: Get Yarn version
        run: echo "::set-output name=YARN_VERSION::$(yarn --version)"
        id: yarn-version
      - name: Print Yarn version
        run: yarn --version
      - name: Cache yarn dependencies
        uses: actions/cache@v2
        with:
          path: ${{ steps.yarn-cache-dir.outputs.YARN_CACHE_DIR }}
          key: yarn-cache-${{ runner.os }}-yarn-${{ steps.yarn-version.outputs.YARN_VERSION }}-${{ hashFiles('yarn.lock') }}
      - name: Install dependencies
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn --immutable --immutable-cache
      - name: Build
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn build
      - name: Lint
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn lint
      - name: Test
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn test
      - name: Validate RC changelog
        if: ${{ startsWith(github.head_ref, 'release/') }}
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn lint:changelog --rc
      - name: Validate changelog
        if: ${{ !startsWith(github.head_ref, 'release/') }}
        run: |
          echo "$(dirname $(which yarn))" >> $GITHUB_PATH
          yarn lint:changelog
      - name: Require clean working directory
        shell: bash
        run: |
          if ! git diff --exit-code; then
            echo "Working tree dirty after building"
            exit 1
  all-jobs-pass:
    name: All jobs pass
    runs-on: ubuntu-20.04
    needs:
      - build-lint-test
    steps:
      - run: echo "Great success!"
